import { GlobalNetworkContent } from "@/components/global-network-content"

// Separate file for metadata (server component)
export const metadata = {
  title: "Global Network | Zinerva Energy",
  description: "Our worldwide distribution infrastructure and logistics capabilities"
}

// Main page component (server component)
export default function GlobalNetworkPage() {
  return <GlobalNetworkContent />
} 